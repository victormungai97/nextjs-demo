import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About | NextJS Demo",
  description: "It's the about page",
};

const About = () => {
  throw Error("Not Implemented");

  return (
    <div>
      <h1>About</h1>
    </div>
  );
};

export default About;
